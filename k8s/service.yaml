apiVersion: v1
kind: Service
metadata:
  name: cloud-platform-api
spec:
  selector:
    app: cloud-platform-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
